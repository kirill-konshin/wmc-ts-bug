{
  "name": "web-modules-all",
  "version": "0.0.0",
  "repository": {
    "private": true
  },
  "scripts": {
    "clean": "lerna run clean && lerna clean --yes && rimraf node_modules",
    "postinstall": "lerna bootstrap --hoist"
  },
  "author": "RingCentral",
  "license": "ISC",
  "dependencies": {
    "cross-env": "5.2.0",
    "lerna": "3.15.0"
  }
}
